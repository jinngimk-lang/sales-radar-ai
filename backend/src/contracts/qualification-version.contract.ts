/**
 * Qualification decisions are persisted snapshots. Increment this value when
 * production trust rules change so older decisions remain auditable without
 * being treated as current.
 */
export const CURRENT_QUALIFICATION_VERSION = 'v2'
export const LEGACY_QUALIFICATION_VERSION = 'v1'
