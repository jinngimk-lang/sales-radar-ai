/**
 * Backward-compatible export. New providers should import the explicitly
 * named interface module.
 */
export type {
  SearchProvider,
  SearchProviderInput,
  SearchProviderName,
  SearchResult,
} from './search-provider.interface.js'
